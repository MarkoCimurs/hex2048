export class Hexagon {
  x: number;
  y: number;
  z: number;
  value: number;

  constructor(x: number, y: number, z: number, value: number) {
    this.x = x;
    this.y = y;
    this.z = z;
    this.value = value;
  }
}
